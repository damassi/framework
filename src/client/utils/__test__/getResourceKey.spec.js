/* eslint no-unused-vars: 0 */
import { expect } from 'chai'
import getResourceKey from 'client/utils/getResourceKey'
import { resources } from 'shared/configuration'

describe('(client/utils/getResourceKey.js)', () => {
  it('TODO: should return resource key from router path', () => {
    // expect(
    //   getResourceKeys()
    // ).to.eql(resources.map(x => x.resourceKey))
  })
})

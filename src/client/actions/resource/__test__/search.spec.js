/* eslint no-unused-vars: 0 */

// import Pretender from 'pretender'
// import thunk from 'redux-thunk'
// import initSearch from 'client/actions/resource/search'
// import configureStore from 'client/utils/helpers/mockStore'

// FIXME: Figure out way to test Elastic Search API

describe('(client/actions/resource/search.js)', () => {

  beforeEach(() => {
  })

  afterEach(() => {
  })

  it('TODO: Should dispatch `search` API calls', (done) => {
    done()
  })
})
